"""Baseline trainer placeholders."""

from __future__ import annotations


def train_progression_baseline() -> None:
    raise RuntimeError("Progression baseline training requires a validated manifest with labels and PyTorch/scikit-learn installed.")
