import type { Metadata } from 'next';
export const metadata:Metadata={title:'RetinaProgress · SIGF review',description:'Advisor review of 40 curated longitudinal image series.',robots:{index:false,follow:false},openGraph:{title:'RetinaProgress · SIGF review',description:'Advisor image review',images:[]},twitter:{card:'summary',images:[]}};
export default function Layout({children}:{children:React.ReactNode}){return children;}
